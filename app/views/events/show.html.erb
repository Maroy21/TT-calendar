<%= @event.date %>
<br>
<%= @event.body %>
<br>

<ul style='margin-top : 1rem'>
  <li class="btn btn-secondary"><%= link_to "Edit", edit_event_path(@event) %></li>
  <li class="btn btn-danger"><%= link_to "Destroy", event_path(@event),
                  method: :delete,
                  data: { confirm: "Are you sure?" } %></li>
</ul>